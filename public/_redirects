# Cloudflare Pages redirects.
#
# NOTE: with @cloudflare/next-on-pages this file is NOT honored — the
# generated _worker.js intercepts every route before Pages consults
# _redirects. The actual redirect rules live in next.config.ts under
# redirects(), which is baked into the worker bundle. This file is
# kept so anyone reading the project remembers the rule already fired
# in next.config.ts; do NOT add new rules here.
